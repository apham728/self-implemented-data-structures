# Time Complexity Notes

## Lists (Unordered)
- ArrayList: insert() = O(1); find() = O(n); remove() = O(n)
- LinkedList: insert() = O(n); find() = O(n); remove() = O(n)
    - LinkedList insert() at head = O(1)